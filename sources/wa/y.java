package wa;

import java.util.Locale;

public final class y extends Exception {
    public y(String str) {
        super(str);
        if (str != null) {
            str.toLowerCase(Locale.US).getClass();
        }
    }
}
